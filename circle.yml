machine:
  node:
    version: 4.4.2
  services:
    - docker
dependencies:
  override:
    - make dependencies
test:
  override:
    - make lint
    - make clean
    - make
    - make test
deployment:
  production:
    branch: master
    commands:
      - ./bin/deploy.sh
